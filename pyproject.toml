[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psycopg2>=2.9.10",
    "scrapy>=2.12.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
